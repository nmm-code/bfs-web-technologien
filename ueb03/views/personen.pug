extends layout

block content

  div.personen-container
    each person in personen
      div.person
        p Name: #{person.name}
        p E-Mail: #{person.email}
        p Adresse: #{person.address.suite}, #{person.address.street}, #{person.address.city}, #{person.address.zipcode}